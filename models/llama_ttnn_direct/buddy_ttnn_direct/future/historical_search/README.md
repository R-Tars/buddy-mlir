# Historical search

This directory preserves phase-era validation compatibility only. It is not
imported by the product command path and must not be used for performance
tuning. Current hardware tuning lives in `diagnostics/autotune.py` and uses
post-prefill steady decode measurements.
